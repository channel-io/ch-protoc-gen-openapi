syntax = "proto3";

package test14;

enum Color {
  COLOR_UNSPECIFIED = 0;
  COLOR_RED = 1;
  COLOR_BLUE = 2;
}

message Inner {
  string id = 1;

  // +kubebuilder:example="red"
  Color color = 2;
}

message Outer {
  Inner inner = 1;

  repeated Inner items = 2;
}
